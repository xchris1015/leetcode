package String.easy;

public class Anagram {

    // sort str1 and str2 to check if they are the same, if input has pattern, could use bucket sort
    // could also use hash to check with weight, the celling is the input n, might able to use n if there is
    // no input pattern


    // how about group anagram?
    // S1 : because every anagram sorted are equal, we need to find a one(sorted) to List<String> map
    // for each string, sort and check hashmap contains, if yes, put, if not create


    // S2: Union find

    // what if input is too big? mapReduce
}
